// 기본 데이터 타입

// 아래 타입을 포함한 거의 대부분의 타입들이 객체로 이루어져 있다. (함수도 객체)
// 이것이 Dart가 진정한 객체 지향 언어로 불리는 이유이다.

void main() {
  String name = "tom";
  bool isPlay = true;
  int age = 10;
  double money = 52.55;
  num x = 12;
  num y = 1.2;
}
